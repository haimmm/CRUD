/**
 * User - standard permission, can access it's profile and adopt pets. 
 * Admin - can access admin dashboard, add and edit pets.
 */

const permissions = {
    USER: "user",
    ADMIN: "Admin"
}

module.exports = permissions;